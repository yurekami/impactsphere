import { StubPage } from './StubPage'

export default function EventsPage() {
  return (
    <StubPage
      title="Events"
      description="Normalized event stream from commits, issues, incidents, and policy feeds."
    />
  )
}
